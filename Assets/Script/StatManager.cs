using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatManager : MonoBehaviour
{
    // Start is called before the first frame update
    private Dictionary<StatType, int> _originStatD = new Dictionary<StatType, int>();
    private Dictionary<StatType, int> _moddedStatD = new Dictionary<StatType, int>();

    public int HP;  

    private void  setupHP()
    {

    }

   public void GetTotalStat(StatType statType)
    {
        
    }

    private void SetupOriginalStat()
    {
        //foreach( var statField in typeof )
      }


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
